<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de lista para órdenes de reparación -->
    <record id="view_repair_order_tree" model="ir.ui.view">
        <field name="name">mobile.repair.order.tree</field>
        <field name="model">mobile.repair.order</field>
        <field name="arch" type="xml">
            <list decoration-info="status=='draft'" 
                  decoration-warning="status=='in_progress'" 
                  decoration-success="status=='completed'"
                  decoration-muted="status=='canceled'">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="device_id"/>
                <field name="technician_id"/>
                <field name="priority"/>
                <field name="repair_date"/>
                <field name="delivery_date"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario para órdenes de reparación -->
    <record id="view_repair_order_form" model="ir.ui.view">
        <field name="name">mobile.repair.order.form</field>
        <field name="model">mobile.repair.order</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_start_repair" string="Iniciar Reparación" 
                            type="object" class="btn-primary" 
                            invisible="status != 'draft'"/>
                    <button name="action_complete" string="Completar" 
                            type="object" class="btn-primary" 
                            invisible="status != 'in_progress'"/>
                    <button name="action_cancel" string="Cancelar" 
                            type="object" 
                            invisible="status in ['completed', 'canceled']"/>
                    <button name="action_reset_to_draft" string="Regresar a Borrador" 
                            type="object" 
                            invisible="status == 'draft'"/>
                    <field name="status" widget="statusbar" 
                           statusbar_visible="draft,in_progress,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Información del Cliente">
                            <field name="customer_id"/>
                            <field name="repair_date"/>
                            <field name="delivery_date"/>
                        </group>
                        <group string="Información Técnica">
                            <field name="device_id"/>
                            <field name="technician_id"/>
                            <field name="priority"/>
                        </group>
                    </group>
                    <group>
                        <group string="Costos">
                            <field name="estimated_cost"/>
                            <field name="actual_cost"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Descripción del Problema">
                            <field name="description" placeholder="Describe el problema reportado por el cliente..."/>
                        </page>
                        <page string="Notas de Reparación">
                            <field name="notes" placeholder="Notas técnicas sobre la reparación..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Vista kanban para órdenes de reparación -->
    <record id="view_repair_order_kanban" model="ir.ui.view">
        <field name="name">mobile.repair.order.kanban</field>
        <field name="model">mobile.repair.order</field>
        <field name="arch" type="xml">
            <kanban default_group_by="status">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="device_id"/>
                <field name="priority"/>
                <field name="status"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                    <div class="o_kanban_record_top_right">
                                        <div t-if="record.priority.raw_value == 'urgent'" 
                                             class="badge badge-danger">Urgente</div>
                                        <div t-elif="record.priority.raw_value == 'high'" 
                                             class="badge badge-warning">Alta</div>
                                    </div>
                                </div>
                                <div class="o_kanban_record_body">
                                    <field name="customer_id"/>
                                    <br/>
                                    <field name="device_id"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Acción principal -->
    <record id="action_repair_order_form_view" model="ir.actions.act_window">
        <field name="name">Órdenes de Reparación</field>
        <field name="res_model">mobile.repair.order</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crea tu primera orden de reparación móvil.
            </p>
            <p>
                Las órdenes de reparación te permiten gestionar todo el proceso 
                de reparación de dispositivos móviles desde el ingreso hasta la entrega.
            </p>
        </field>
    </record>
</odoo>